#include <stdio.h>
#include <stdlib.h>

int main()
{
    int x = 5;
    int* p = NULL;
    printf("Value *p is %d", p);
    // int** q = p;
    // q = &p;
    //  printf("Value *q is %d", *q);
    return 0;
}